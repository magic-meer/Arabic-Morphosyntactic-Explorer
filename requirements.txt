# Core dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
google-genai>=1.0.0
httpx>=0.28.0

# Arabic NLP & RAG
camel-tools>=1.2.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
numpy<2.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0

# Optional heavy dependencies
# Install only if needed: pip install -r requirements-optional.txt

# Arabic morphological analysis (CAMeL Tools)
# Note: Requires Java and specific setup - see https://github.com/CAMeL-Lab/camel_tools
camel-tools>=1.4.0

# Vector database for RAG (ChromaDB)
# Alternative: Could use FAISS or simple JSON file for small datasets
chromadb>=0.4.0

# Sentence embeddings for semantic search
# Heavy (pulls ~2GB torch) - use only if you need neural embeddings
# Alternative: Use TF-IDF (sklearn) or BM25 for lighter search
sentence-transformers>=2.2.0

# Development tools (already in main requirements)
# ruff==0.1.11
# mypy==1.8.0
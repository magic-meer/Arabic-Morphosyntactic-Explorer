# Essential dependencies (compatible with Python 3.14)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
google-generativeai>=0.8.0
httpx>=0.28.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0

# Optional (install separately): pip install -r requirements-optional.txt
# - camel-tools (Arabic morphology - heavy but powerful)
# - chromadb (Vector database for RAG - can use simpler alternative)
# - sentence-transformers (Embeddings - pulls torch, very heavy)